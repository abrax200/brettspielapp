import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.brettspiel.app',
  appName: 'brettspielapp',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
